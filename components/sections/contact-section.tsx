import { SectionContainer } from "./section-container";

export function ContactSection() {
  return (
    <SectionContainer id="contato" title="Contato">
      <p>Texto, formulário e etc...</p>
    </SectionContainer>
  );
}
